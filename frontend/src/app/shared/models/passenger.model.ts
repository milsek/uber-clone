export interface Passenger {
  username: string,
  name: string,
  surname: string,
  phoneNumber: string,
  city: String,
  profilePicture: string,
  distanceTravelled: number,
  ridesCompleted: number,
  tokenBalance: number,
  email?: string,
  accountType?: string,
}
